name: ci-node-sonarcloud
on: 
  push:
    branches: [ develop ]
jobs:
  check-application:
    runs-on: ubuntu-latest
    strategy:
      matrix: 
        action: ['./actions/node14', './actions/node16']
    steps:
      - run: echo 'Hi ${{ job.container.id }}'
      - run: echo 'Hi ${{ env.node-action }}'
        env:
          node-action: ${{ matrix.action }}
      # - uses: actions/checkout@v3
      # - uses: actions/setup-node@v3
      #   with:
      #     node-version: ${{ matrix.node }}
      # - run: npm install
      # - run: npm test
      # - run: node index
      # - run: curl https://viacep.com.br/ws/01001000/json/
      - name: Hello world action step
        uses: ${{ env.node-action }}
        id: hello
        with:
          who-to-greet: 'Carlos'

